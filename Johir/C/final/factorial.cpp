#include <stdio.h>
#include <conio.h>
main()
 {
      int i=5,j=4,f,fac(i);
      printf("factorial is %d",);
      scanf("%d",fac(i));
      printf("factorial is %d",);
      scanf("%d",fac(j));
      for(i=1;i<=5;i++)
      {
      for(j=1;j<=4;j++)
      {
      printf("factorial is %d",fac(j));
      }
      printf("factorial is %d"fac(i));
      }
      printf("factorial of%d is %d",i,fac(i));
      printf("factorial of%d is %d",j,fac(j));
      getch();
      }
